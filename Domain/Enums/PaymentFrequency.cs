namespace Domain.Enums;

public enum PaymentFrequency
{
    Daily = 1, // Ежедневно
    Weekly = 2, // Еженедельно
    Monthly = 3, // Ежемесячно
    Yearly = 4, // Ежегодно
    Custom = 5 // Произвольный период
}
